// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * A Cluster Queue is a queue belonging to a specific Cluster for its Agents to target builds on.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as buildkite from "@pulumiverse/buildkite";
 *
 * // create a cluster
 * const primary = new buildkite.cluster.Cluster("primary", {
 *     name: "Primary cluster",
 *     description: "Runs the monolith build and deploy",
 *     emoji: "🚀",
 *     color: "#bada55",
 * });
 * const monolith = new buildkite.pipeline.Pipeline("monolith", {
 *     name: "Monolith",
 *     repository: "https://github.com/...",
 *     clusterId: primary.id,
 * });
 * // create a queue to put pipeline builds in
 * const _default = new buildkite.cluster.ClusterQueue("default", {
 *     clusterId: primary.id,
 *     key: "default",
 *     dispatchPaused: true,
 * });
 * // create a hosted agent queue with macos agents
 * const hostedAgentsMacos = new buildkite.cluster.ClusterQueue("hosted_agents_macos", {
 *     clusterId: primary.id,
 *     key: "hosted-agents-macos",
 *     dispatchPaused: true,
 *     hostedAgents: {
 *         instanceShape: "MACOS_ARM64_M4_6X28",
 *     },
 * });
 * // create a hosted agent queue with linux agents
 * const hostedAgentsLinux = new buildkite.cluster.ClusterQueue("hosted_agents_linux", {
 *     clusterId: primary.id,
 *     key: "hosted-agents-linux",
 *     dispatchPaused: true,
 *     hostedAgents: {
 *         instanceShape: "LINUX_AMD64_2X4",
 *         linux: {
 *             agentImageRef: "ubuntu:24.04",
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import resources using the `id`. For example:
 *
 * import a cluster queue resource using the GraphQL ID along with its respective cluster UUID
 *
 * you can use this query to find the ID:
 *
 * query getClusterQueues {
 *
 *   organization(slug: "ORGANIZATION_SLUG") {
 *
 *     cluster(id: "CLUSTER_UUID") {
 *     
 *       queues(first: 50) {
 *     
 *         edges {
 *     
 *           node {
 *     
 *             id
 *     
 *             key
 *     
 *           }
 *     
 *         }
 *     
 *       }
 *     
 *     }
 *
 *   }
 *
 * }
 *
 * ```sh
 * $ pulumi import buildkite:Cluster/clusterQueue:ClusterQueue test Q2x1c3RlclF1ZXVlLS0tYjJiOGRhNTEtOWY5My00Y2MyLTkyMjktMGRiNzg3ZDQzOTAz,35498aaf-ad05-4fa5-9a07-91bf6cacd2bd
 * ```
 */
export class ClusterQueue extends pulumi.CustomResource {
    /**
     * Get an existing ClusterQueue resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ClusterQueueState, opts?: pulumi.CustomResourceOptions): ClusterQueue {
        return new ClusterQueue(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'buildkite:Cluster/clusterQueue:ClusterQueue';

    /**
     * Returns true if the given object is an instance of ClusterQueue.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ClusterQueue {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ClusterQueue.__pulumiType;
    }

    /**
     * The ID of the cluster that this cluster queue belongs to.
     */
    declare public readonly clusterId: pulumi.Output<string>;
    /**
     * The UUID of the cluster this queue belongs to.
     */
    declare public /*out*/ readonly clusterUuid: pulumi.Output<string>;
    /**
     * A description for the cluster queue.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The dispatch state of a cluster queue.
     */
    declare public readonly dispatchPaused: pulumi.Output<boolean>;
    /**
     * Control the settings for the Buildkite hosted agents.
     */
    declare public readonly hostedAgents: pulumi.Output<outputs.Cluster.ClusterQueueHostedAgents | undefined>;
    /**
     * The key of the cluster queue.
     */
    declare public readonly key: pulumi.Output<string>;
    /**
     * Specifies which agent should be preferred when a job is retried. Valid values are `prefer-warmest` (prefer agents that have recently finished jobs) and `prefer-different` (prefer a different agent if available). Defaults to `prefer-warmest`.
     */
    declare public readonly retryAgentAffinity: pulumi.Output<string>;
    /**
     * The UUID of the cluster queue.
     */
    declare public /*out*/ readonly uuid: pulumi.Output<string>;

    /**
     * Create a ClusterQueue resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ClusterQueueArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ClusterQueueArgs | ClusterQueueState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ClusterQueueState | undefined;
            resourceInputs["clusterId"] = state?.clusterId;
            resourceInputs["clusterUuid"] = state?.clusterUuid;
            resourceInputs["description"] = state?.description;
            resourceInputs["dispatchPaused"] = state?.dispatchPaused;
            resourceInputs["hostedAgents"] = state?.hostedAgents;
            resourceInputs["key"] = state?.key;
            resourceInputs["retryAgentAffinity"] = state?.retryAgentAffinity;
            resourceInputs["uuid"] = state?.uuid;
        } else {
            const args = argsOrState as ClusterQueueArgs | undefined;
            if (args?.clusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'clusterId'");
            }
            if (args?.key === undefined && !opts.urn) {
                throw new Error("Missing required property 'key'");
            }
            resourceInputs["clusterId"] = args?.clusterId;
            resourceInputs["description"] = args?.description;
            resourceInputs["dispatchPaused"] = args?.dispatchPaused;
            resourceInputs["hostedAgents"] = args?.hostedAgents;
            resourceInputs["key"] = args?.key;
            resourceInputs["retryAgentAffinity"] = args?.retryAgentAffinity;
            resourceInputs["clusterUuid"] = undefined /*out*/;
            resourceInputs["uuid"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ClusterQueue.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ClusterQueue resources.
 */
export interface ClusterQueueState {
    /**
     * The ID of the cluster that this cluster queue belongs to.
     */
    clusterId?: pulumi.Input<string>;
    /**
     * The UUID of the cluster this queue belongs to.
     */
    clusterUuid?: pulumi.Input<string>;
    /**
     * A description for the cluster queue.
     */
    description?: pulumi.Input<string>;
    /**
     * The dispatch state of a cluster queue.
     */
    dispatchPaused?: pulumi.Input<boolean>;
    /**
     * Control the settings for the Buildkite hosted agents.
     */
    hostedAgents?: pulumi.Input<inputs.Cluster.ClusterQueueHostedAgents>;
    /**
     * The key of the cluster queue.
     */
    key?: pulumi.Input<string>;
    /**
     * Specifies which agent should be preferred when a job is retried. Valid values are `prefer-warmest` (prefer agents that have recently finished jobs) and `prefer-different` (prefer a different agent if available). Defaults to `prefer-warmest`.
     */
    retryAgentAffinity?: pulumi.Input<string>;
    /**
     * The UUID of the cluster queue.
     */
    uuid?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ClusterQueue resource.
 */
export interface ClusterQueueArgs {
    /**
     * The ID of the cluster that this cluster queue belongs to.
     */
    clusterId: pulumi.Input<string>;
    /**
     * A description for the cluster queue.
     */
    description?: pulumi.Input<string>;
    /**
     * The dispatch state of a cluster queue.
     */
    dispatchPaused?: pulumi.Input<boolean>;
    /**
     * Control the settings for the Buildkite hosted agents.
     */
    hostedAgents?: pulumi.Input<inputs.Cluster.ClusterQueueHostedAgents>;
    /**
     * The key of the cluster queue.
     */
    key: pulumi.Input<string>;
    /**
     * Specifies which agent should be preferred when a job is retried. Valid values are `prefer-warmest` (prefer agents that have recently finished jobs) and `prefer-different` (prefer a different agent if available). Defaults to `prefer-warmest`.
     */
    retryAgentAffinity?: pulumi.Input<string>;
}
